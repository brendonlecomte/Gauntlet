#include "gauntlet.h"

/**
 * \brief   This function needs to search an ordered buffer for a value. The
 *			algorithim must be a Binary Search. The code should handle edge 
 *			cases, be robust to misuse, and be optimised for a memory constrained
 *			target
 * \param   a      Pointer to a sorted buffer of unsigned values
 * \param   len    The length of the buffer a
 * \param   val    The value we are searching for in a
 * \return  int	   -1 for a failure/not present or the index of the array if the value is found
 **/
int binary_search(const unsigned* a, unsigned len, unsigned val)
{

	return -1;
}